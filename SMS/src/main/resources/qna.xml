<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="smsqnans">
	<!-- Use type aliases to avoid typing the full classname every time. -->
	<resultMap id="smsqnaResult"			type="smsqna">
		<result property="sb_no"			column="sb_no" />
		<result property="mem_no"			column="mem_no" />
		<result property="qna_cont"	column="qna_cont" />
		<result property="qna_date"		column="qna_date" />
	</resultMap>
	<insert id="insert" parameterType="smsqna">
		insert into Sms_qna values (#{sb_no}, #{mem_no}, #{qna_cont}, sysdate)
	</insert>
		
<!-- 	<select id="list" parameterType="int" resultMap="smsreqreplyResult">
		select * from Sms_request_reply where rq_no=#{rq_no} order by rq_re_no
	</select>
	<insert id="insert" parameterType="smsreqreply">
		<selectKey keyProperty="rq_re_no" order="BEFORE" resultType="int">
			select nvl(max(rq_re_no), 0) + 1 from Sms_request_reply
		</selectKey>
		insert into Sms_request_reply values (#{rq_no}, #{rq_re_no}, #{rq_re_cont}, #{mem_no}, sysdate, sysdate, 'n')
	</insert>
	<update id="delete" parameterType="int">
		update Sms_request_reply set del='y' where rq_re_no=#{rq_re_no}
	</update>
	
	<update id="update" parameterType="smsreqreply">
		update Sms_request_reply set rq_re_cont=#{rq_re_cont}, updatedate=sysdate where rq_re_no=#{rq_re_no}
	</update> -->
</mapper>